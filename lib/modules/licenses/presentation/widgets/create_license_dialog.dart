import 'package:flutter/material.dart';

/// Dialog to create a new license.
class CreateLicenseDialog extends StatelessWidget {
  /// Dialog to create a new license.
  const CreateLicenseDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
